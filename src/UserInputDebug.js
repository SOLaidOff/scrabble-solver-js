import React, { Component } from 'react';

class UserInputDebug extends React.Component {
  render() {
    return (
        <span>{this.props.userInput}</span>
    );
  }
}

export default UserInputDebug;
